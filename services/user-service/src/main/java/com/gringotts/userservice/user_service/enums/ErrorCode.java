package com.gringotts.userservice.user_service.enums;

public enum ErrorCode {

    VALIDATION_ERROR,
    USER_NOT_FOUND,
    USER_ALREADY_EXISTS,
    IDENTITY_PROVIDER_ERROR,
    AUTHENTICATION_FAILED,
    ACCESS_DENIED,
    INTERNAL_SERVER_ERROR
}